export interface OpenWhenLetter {
  id: string;
  title: string;
  emoji: string;
  color: string;
  content: string[];
  closing: string;
}

export interface Category {
  id: string;
  name: string;
  description: string;
}
